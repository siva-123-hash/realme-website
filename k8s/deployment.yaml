apiVersion: apps/v1
kind: Deployment
metadata:
  name: realme-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: realme-app
  template:
    metadata:
      labels:
        app: realme-app
    spec:
      containers:
        - name: realme-app
          image: yourdockerhubusername/realme-node-website:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
