apiVersion: v1
kind: Service
metadata:
  name: realme-service
spec:
  selector:
    app: realme-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
